# Pipeline Automation - API-Only Environment Dependencies
#
# This is for running ONLY the FastAPI management API without Airflow
# This environment is compatible with dataforge shared libraries

# API Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Shared libraries (compatible with Pydantic v2, SQLAlchemy 2.0)
# Install separately with:
#   pip install -e ../../shared/common
#   pip install -e ../../shared/connectors

# Data processing
pandas>=2.0.0
pyarrow>=13.0.0
sqlalchemy>=2.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
